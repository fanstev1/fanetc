% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_time_desp.R
\name{add_atrisk}
\alias{add_atrisk}
\title{add_atrisk}
\usage{
add_atrisk(
  p,
  surv_obj,
  x_break = NULL,
  atrisk_init_pos = NULL,
  plot_theme = NULL
)
}
\arguments{
\item{p}{a ggplot object showing the survival or cumulative-incidence curves}

\item{surv_obj}{the survfit object the plot was built from}

\item{x_break}{a numeric vector of time points at which the at-risk counts are displayed (default: the plot's x-axis breaks)}

\item{atrisk_init_pos}{position of the "At-risk N:" header, in text lines below the panel bottom. Default: with more than one group the header starts on the line right after the x-axis title (2.23); with a single group it sits one full line of whitespace below it (3.06)}

\item{plot_theme}{a ggplot2 theme object; its text family, face and size are used for the table text (default: plain 11pt Arial)}
}
\value{
a ggplot object with the at-risk table added
}
\description{
add_atrisk
}
\details{
The function adds an at-risk table underneath a survival or cumulative-incidence
plot. The horizontal positions are data coordinates (the time points); the
vertical positions are absolute text lines below the panel bottom, so the row
spacing does not change with figure or panel size. The annotations are drawn
outside the panel, so the figure needs a sufficient bottom/left plot.margin and
the panel clip turned off to show them (see the show_cif() example).
}
