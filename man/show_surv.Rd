% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_time_desp.R
\name{show_surv}
\alias{show_surv}
\title{show_surv}
\usage{
show_surv(
  surv_obj,
  x_lab = "Time",
  y_lab = if (plot_cdf) "The proportion of deceased subjects" else
    "The freedom from death",
  y_lim = NULL,
  x_break = NULL,
  y_break = NULL,
  color_scheme = c("brewer", "grey", "viridis", "manual"),
  color_list = NULL,
  plot_theme = theme_minimal(),
  add_ci = TRUE,
  add_atrisk = TRUE,
  add_legend = FALSE,
  add_pvalue = TRUE,
  atrisk_init_pos = NULL,
  pvalue_pos = c("topleft", "topright", "bottomleft", "bottomright", "left", "right",
    "top", "bottom"),
  plot_cdf = FALSE,
  print_fig = TRUE
)
}
\arguments{
\item{surv_obj}{a survfit object, as returned by estimate_km().}

\item{x_lab}{the x-axis label.}

\item{y_lab}{the y-axis label; the default depends on plot_cdf.}

\item{y_lim}{a numeric vector of length 2 specifying the y-axis limits; the lower limit is always reset to 0, and NULL gives c(0, 1). Curves are truncated at an upper limit below 1.}

\item{x_break}{a numeric vector specifying the x-axis break points (default: automatic).}

\item{y_break}{a numeric vector specifying the y-axis break points (default: automatic).}

\item{color_scheme}{the color palette used for strata: "brewer", "grey", "viridis", or "manual".}

\item{color_list}{a list of scale arguments, required only when color_scheme= 'manual' (e.g. list(values= c('red', 'blue'))).}

\item{plot_theme}{a ggplot2 theme object applied to the plot.}

\item{add_ci}{a logical parameter indicating whether confidence interval ribbons should be added to the plot.}

\item{add_atrisk}{a logical parameter indicating whether at-risk table should be added to the figure.}

\item{add_legend}{a logical parameter indicating whether legend should be added to the figure; forced to FALSE for a single cohort or when the at-risk table is shown (the table is color-coded by cohort instead).}

\item{add_pvalue}{a logical parameter indicating whether the log-rank p-value (from run_logrank_test()) should be added to the plot; forced to FALSE for a single cohort.}

\item{atrisk_init_pos}{position of the "At-risk N:" header, in text lines below the panel bottom. Default: with more than one group the header starts on the line right after the x-axis title (2.23); with a single group it sits one full line of whitespace below it (3.06)}

\item{pvalue_pos}{a character parameter indicating where the p-value should be added to the plot.}

\item{plot_cdf}{a logical parameter indicating whether the failure function 1 - S(t) should be plotted instead of the survival function.}

\item{print_fig}{a logical parameter indicating whether the figure should be printed to the active device.}
}
\value{
A ggplot object.
}
\description{
show_surv
}
\details{
The function shows Kaplan-Meier survival (or failure) curves with and without
strata, with optional confidence-interval ribbons, an at-risk table and the
log-rank p-value.
}
